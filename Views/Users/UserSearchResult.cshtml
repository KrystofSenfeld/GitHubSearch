@model GitHubSearch.Models.Users.UserModel

@{
    ViewBag.Title = "Search Result";
}

<main>
    <div class="mb-3">
        <a href='@Url.Action("Index", "Users")' class="btn btn-secondary">
            <i class="fa-solid fa-arrow-left"></i> Back
        </a>
    </div>

    <!-- SUMMARY -->
    <div class="alert alert-secondary" role="alert">
        <div class="d-flex flex-row">
            <div class="me-3">
                <img src="@Model.AvatarUrl" class="img-thumbnail" height="150" width="150" />
            </div>

            <div>
                <h1><a href="@Model.Url" class="alert-link">@Model.Name</a></h1>
                @Html.DisplayFor(m => m.Location)
            </div>
        </div>
    </div>

    <!-- REPOS -->
    <div>
        <h3>Repos</h3>

        @if (!Model.HasRepos) {
            <p>This user has no public repositories.</p>
        } else {
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Description</th>
                        <th scope="col">Stargazers</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var repo in Model.Repos) {
                        <text>
                            <tr>
                                <th scope="row"><a href="@repo.Url">@repo.Name</a></th>
                                <td>@repo.Description</td>
                                <td>@repo.StargazersCount</td>
                            </tr>
                        </text>
                    }
                </tbody>
            </table>
        }
    </div>
</main>